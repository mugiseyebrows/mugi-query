edit schema dialog
users helper
save data:
    values(),(),()
    backticks (mysql)
editable shortcuts
beautify query
upcase keywords
query history: 
    save not executed on close session?
    exec and error columns?
    highlight
    search tables table insert/select/update/delete/alter/any filters
find-static-functions.py
tokens: 
    ms access functions and types
    store in file
copyeventfilter: refactor
textedit: 
    functions arguments popup
    alias tokens (subqueries)
    autocomplete before and after '(),;'
relations: 
    fix double join
    refactor
dialogs:
    this->adjustSize();
    tab order
join helper:
    relations, tables: copy/paste/delete event filter
    reorder tables buttons
    deduce relations %table_name%_id
plot data
result tab:
    context menu: selection menu
adddatabase:
    search filters
fix save data 'select * from (((foo)))'
qsharedpointer for query models?
csv/tsv to sql
code layout
    models
    widgets
    generated
    utilities
plot:
    detachable
    x range filter: auto, manual
    title, axis titles
    clickable bars
    clickable points
    xy plot zoom and pan
    cursor values
    xy plot x as datatime
memleaks
clean disconnect
more colors
