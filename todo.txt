edit schema dialog
users helper
save data:
    values(),(),()
    backticks (mysql)
editable shortcuts
beautify query
upcase keywords
query history: 
    save not executed on close session?
    exec and error columns?
    highlight
    search tables table insert/select/update/delete/alter/any filters
    copy many
find-static-functions.py
tokens:
    ms access functions and types
    store in file
copyeventfilter: refactor
textedit: 
    functions arguments popup
    alias tokens (subqueries)
    autocomplete before and after '(),;'
relations: 
    fix double join
    refactor
dialogs:
    tab order
join helper:
    relations, tables: copy/paste/delete event filter
    reorder tables buttons
    deduce relations %table_name%_id
adddatabase:
    search filters
    pick file for odbc and sqlite drivers
fix save data 'select * from (((foo)))'
qsharedpointer for query models?
csv/tsv to sql
code layout:
    src/
    src/model    
    src/widget
plot:
    both:
        detachable
        title, axis titles
        show text on hover
        x as datatime
    xy:
        zoom and pan
        legend
    distribution:
        x range filter: auto, manual
        clickable bars
        sigm, m0, gauss curve
        y in percent
memleaks
clean disconnect
color input delegate
combobox delegates with autocompletion instead of lineedit (optional)
class Histogram
save data table name + '.sql' = filename 
save data:
    preview
